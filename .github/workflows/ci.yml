name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pipeline:
    name: Pipeline tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test deps
        run: pip install pytest pyyaml
      - name: Unit tests
        run: python -m pytest pipeline/tests -v
      - name: Validate dbt schema YAML
        run: |
          python -c "import yaml; yaml.safe_load(open('pipeline/models/schema.yml')); yaml.safe_load(open('pipeline/models/sources.yml')); print('dbt yml ok')"

  dashboard:
    name: Dashboard build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
      # Static build check only — placeholder Supabase vars so module-load createClient()
      # doesn't throw. Real NEXT_PUBLIC_* values are set in Vercel for the deployed build.
      - run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key-for-ci-build
